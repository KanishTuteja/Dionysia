# Internal — keep off the public web deploy (no secrets in them, but no need to serve)
supabase/
MEMBERSHIP-SETUP.md
tools/

# Local dev
.claude/
deploy-test.bat
supabase-test.html
*.jsx
 
# Film masters / encodes - served from Cloudflare R2, never commit 
By all means/ 
*.mp4 
*.part* 
